# IP address to bind the proxy service to.
# Must be a routable IP (not loopback)
# SERVICE_IP=$(ifconfig eth0 | grep -Eo "inet addr:[0-9.]+" | grep -Eo "[0-9.]+")

# Port to service the proxy on.
# SERVICE_PORT=18000

# ARN for default IAM role for containers without an explicit role.
# A default role is not required. Containers with no explicit role
# will receive an error when attempting to access credentials.
# DEFAULT_IAM_ROLE=
